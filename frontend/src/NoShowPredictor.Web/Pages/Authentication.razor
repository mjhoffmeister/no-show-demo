@page "/authentication/{action}"

<RemoteAuthenticatorView Action="@ResolvedAction" />

@code {
    [Parameter]
    public string? Action { get; set; }

    private string ResolvedAction => string.IsNullOrWhiteSpace(Action) ? "login" : Action;
}
